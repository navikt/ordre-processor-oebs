apiVersion: nais.io/v1alpha1
kind: Application

metadata:
  name: ordre-processor-oebs-t1
  namespace: team-oebs
  labels:
    team: team-oebs

spec:
  image: {{ image }}
  port: 8080

  env:
    - name: SPRING_PROFILES_ACTIVE
      value: gcp
    # Keep listeners explicitly enabled so one can be toggled off during isolation tests.
    - name: APP_KAFKA_NOMSHENDELSE_AUTO_STARTUP
      value: "true"
    - name: APP_KAFKA_LIVSHENDELSE_AUTO_STARTUP
      value: "true"

  replicas:
    min: 1
    max: 1
    cpuThresholdPercentage: 50
  liveness:
    path: /actuator/health/liveness
    initialDelay: 60
    timeout: 10
  readiness:
    path: /actuator/health/readiness
    initialDelay: 60
    timeout: 10
  resources:
    limits:
      memory: 768Mi
    requests:
      cpu: 200m
      memory: 512Mi
  prometheus:
    enabled: true
    path: /actuator/prometheus
  kafka:
    pool: nav-dev
